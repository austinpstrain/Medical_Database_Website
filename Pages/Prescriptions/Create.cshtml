@page
@model ClinicWeb.Pages.Prescriptions.CreateModel
@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<form class="form-horizontal" method="post">
    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
    <input type="hidden" asp-for="Patient.PatientId" />
    <div class="row">
        <div class="col-md-4">
            <div class="form-group">
                @Html.LabelFor(m => m.Prescription.Product, new { @class = "control-label" })
                @Html.EditorFor(m => m.Prescription.Product)
                @Html.ValidationMessageFor(m => m.Prescription.Product)
            </div>
        </div>
        <div class="col-md-4">
            <div class="form-group">
                @Html.LabelFor(m => m.Prescription.Dosage, new { @class = "control-label" })
                @Html.EditorFor(m => m.Prescription.Dosage)
                @Html.ValidationMessageFor(m => m.Prescription.Dosage)
            </div>
        </div>
    </div>
    <div class="row">
        <div class="col-md-4">
            <div class="form-group">
                @Html.LabelFor(m => m.Prescription.Refill, new { @class = "control-label" })
                @Html.EditorFor(m => m.Prescription.Refill)
                @Html.ValidationMessageFor(m => m.Prescription.Refill)
            </div>
        </div>
        <div class="col-md-4">
            <div class="form-group">
                @Html.LabelFor(m => m.Prescription.Instructions, new { @class = "control-label" })
                @Html.EditorFor(m => m.Prescription.Instructions)
                @Html.ValidationMessageFor(m => m.Prescription.Instructions)
            </div>
        </div>
        <div class="col-md-4">
            <div class="form-group">
                <label>Doctor</label>
                <select asp-for="Prescription.DoctorId">
                    @foreach (var doctor in Model.Doctor)
                    {
                        <option value="@doctor.DoctorId">@doctor.Person.FirstName @doctor.Person.LastName</option>
                    }
                </select>
                @Html.ValidationMessageFor(m => m.Prescription.DoctorId)
            </div>
        </div>
    </div>
    <div class="form-group">
        <input type="submit" value="Create" class="btn btn-primary" />
    </div>

</form>